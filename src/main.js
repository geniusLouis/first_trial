import { createApp } from 'vue'
import App from './App.vue'

import './assets/main.css'

createApp(App).mount('#app')
import { createApp } from 'vue'

app.mount('#app')
app.mount('#trial1')
app.mount('msg')
app.mount('state')

